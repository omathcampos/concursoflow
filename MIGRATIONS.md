# Migrations

Changelog humano das migrations aplicadas ao banco Supabase do ConcursoFlow (ref `apceuvnqnrxfoongjvxq`, projeto **ConcursoFlow**). Atualizado no mesmo commit de cada migration — ver `docs/02-arquitetura.md` para o fluxo completo.

| Arquivo | Descrição | Aplicada em | Ambiente | Fase |
|---|---|---|---|---|
| `20260724124054_initial_schema.sql` | Schema inicial (`docs/03-schema.sql`): enums, tabelas (profiles, subjects, topics, cycles, cycle_entries, blocks, sessions, reviews, annotations), índices, RLS por `user_id`/`auth.uid()` em todas as tabelas, trigger `handle_new_user` para criar profile automático no signup | 2026-07-24 | dev (produção do projeto ConcursoFlow) | 7 |
| `20260724124826_add_schedule_review_to_sessions.sql` | Adiciona `sessions.schedule_review boolean not null default false` — campo já existente no tipo TS `Session` (fase 4/5) que nunca tinha sido incluído em `docs/03-schema.sql`; sem ele o `SupabaseRepository` perderia o dado em cada ida-e-volta. `docs/03-schema.sql` atualizado para refletir a coluna | 2026-07-24 | dev (produção do projeto ConcursoFlow) | 7 |
| `20260724170000_revoke_public_execute_handle_new_user.sql` | Revoga `EXECUTE` de `public`/`anon`/`authenticated` em `public.handle_new_user()` — função de trigger (`on_auth_user_created`) que não deve ser chamável diretamente via `/rest/v1/rpc/handle_new_user`. Corrige alerta `anon/authenticated_security_definer_function_executable` do advisor de segurança | 2026-07-24 | dev (produção do projeto ConcursoFlow) | 8 |
| `20260724211126_notifications.sql` | Tabelas `notification_prefs` (preferências por usuário: tipos habilitados, canais, horário, timezone, `unsubscribe_token`), `notification_log` (idempotência/rate-limit de envio) e `telegram_link_codes` (vínculo de conta do Telegram); RLS em todas; `handle_new_user` passa a criar também a linha de `notification_prefs` default; backfill para usuários já existentes | 2026-07-24 | dev (produção do projeto ConcursoFlow) | 10 |
| `20260724211526_notifications_local_bounds.sql` | Função `notif_local_bounds(timezone, now)`: calcula em SQL (mais confiável que reimplementar em TS) as fronteiras de dia/semana no fuso do usuário, convertidas pra UTC — usada pelas Edge Functions de notificação pra filtrar blocos/sessões/revisões corretamente por fuso. Só `service_role` pode executar | 2026-07-24 | dev (produção do projeto ConcursoFlow) | 10 |
| `20260724212826_notifications_cron.sql` | Habilita `pg_cron`/`pg_net` e agenda 3 jobs (`notif-daily-hourly` de hora em hora, `notif-weekly-daily-check` diário 23h UTC, `notif-overdue-daily` diário 15h UTC) que chamam as Edge Functions via `net.http_post` com header `x-cron-secret`. O valor do secret **não está no arquivo**: vem de `vault.decrypted_secrets` (nome `cron_secret`), inserido separadamente via `vault.create_secret` fora de qualquer migration, pra nunca aparecer em texto plano no repo público | 2026-07-24 | dev (produção do projeto ConcursoFlow) | 10 |
| `20260725010805_notif_local_bounds_fix_search_path.sql` | Fixa `search_path = ''` em `notif_local_bounds` — corrige alerta `function_search_path_mutable` do advisor de segurança (função só usa built-ins do `pg_catalog`, então search_path vazio é seguro) | 2026-07-25 | dev (produção do projeto ConcursoFlow) | 10 |
| `20260727122920_calendar_feed.sql` | Colunas `calendar_feed_token uuid unique default uuid_generate_v4()` e `calendar_feed_include_reviews boolean default false` em `profiles` — feed iCal (.ics) por assinatura da Fase 12. Backfill automático via `default` (todo perfil existente já ganhou um token). Lookup do endpoint público é sempre por token via service role, nunca por `auth.uid()` — RLS de "own profile" não precisa mudar | 2026-07-27 | dev (produção do projeto ConcursoFlow) | 12 |
